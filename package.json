{
  "name": "einvoice-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/mcp"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "corpus": "node scripts/corpus.mjs",
    "gen:codelists": "node scripts/gen-codelists.mjs",
    "site": "node scripts/build-site.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
